@* 

 @using static Core.Enums.Resource_Planing
@model Core.ViewModels.GetAllShiftForAdmin

@{
    ViewData["Title"] = "Assigned Shifts";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Assigned Shifts</h1>
            </div>
        </div>
    </div>
</section>

<section class="content">
    <div class="content container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="table-responsive">
                    <table class="table table-striped custom-table mb-0 datatable">
                        <tbody id="leave">
                            @Html.Raw(Model.ShiftTableHtml)
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>


 *@






 @model Core.ViewModels.GetAllShiftForAdmin
@{
    ViewData["Title"] = "Shift Created";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="page-wrapper">
    <div class="content container-fluid">
        <div class="page-header">
            <h3 class="page-title">Shift</h3>
            <!-- Breadcrumb navigation -->
        </div>

        <!-- Tab navigation -->
        <div class="card tab-box">
            <div class="row user-tabs">
                <div class="col-lg-12 col-md-12 col-sm-12 line-tabs">
                    <ul class="nav nav-tabs nav-tabs-bottom">
                        <li class="nav-item"><a href="#bank_statutory" data-toggle="tab" class="nav-link" data-target="#bank_statutory">Assign Shift</a></li>
                        <li class="nav-item"><a href="#emp_profile" data-toggle="tab" class="nav-link active" data-target="#emp_profile">Name</a></li>
                        <li class="nav-item"><a href="#emp_projects" data-toggle="tab" class="nav-link" data-target="#emp_projects">Time</a></li>
                    </ul>
                </div>
            </div>
        </div>

        <!-- Tab content -->
        <div class="tab-content">
            <!-- Profile Info Tab -->
            <div id="emp_profile" class="pro-overview tab-pane fade show active">
                <!-- Profile info table -->
                @* @Html.Partial("_ProfileInfoTable", Model.ShiftNames) *@
            </div>

            <!-- Projects Tab -->
            <div class="tab-pane fade" id="emp_projects">
                <!-- Project info table -->
                @* @Html.Partial("_ProjectInfoTable", Model.Projects) *@
            </div>

            <!-- Bank Statutory Tab -->
            <div class="tab-pane fade" id="bank_statutory">
                <section class="content">
                    <div class="content container-fluid">
                        <div class="row">
                            <div class="col-md-12">
                                <div class="table-responsive">
                                    <table class="table table-striped custom-table mb-0 datatable">
                                        <tbody id="leave">
                                            @Html.Raw(Model.ShiftTableHtml)
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </section>

            </div>
        </div> 
    </div>
</div>